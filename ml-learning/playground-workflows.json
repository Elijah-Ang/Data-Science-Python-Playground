{
  "source": "ml-app.js · routeForSelection (the ML > Workflow manual walkthrough)",
  "recipes": {
    "gapminder": {
      "simple_linear": {
        "dataset": "gapminder",
        "scenario": "simple",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "gdpPercap"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"gdpPercap\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Original numeric units stay visible so linear coefficients are easier to interpret.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Fit one straight-line relationship\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LinearRegression\n\nmodel = LinearRegression()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate life expectancy for the 2007 country snapshot from GDP per person.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does GDP per person show a training pattern related to life expectancy?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How much does one selected input move the numeric target along a straight line?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final life expectancy error compare with the initial-model CV and any selected-setting CV for Gapminder · 1 continuous feature, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in life expectancy units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      },
      "polynomial": {
        "dataset": "gapminder",
        "scenario": "simple",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "gdpPercap"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"gdpPercap\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# The model scales after polynomial expansion so Ridge regularisation treats terms comparably.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Expand continuous inputs into curved terms, then scale and regularise\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.preprocessing import PolynomialFeatures, StandardScaler\nfrom sklearn.linear_model import Ridge\n\npolynomial = PolynomialFeatures(degree=2, include_bias=False)\nscale = StandardScaler()\nmodel = Ridge()\npipeline = Pipeline([\n    (\"polynomial\", polynomial),\n    (\"scale\", scale),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'polynomial__degree': [2, 3]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate life expectancy for the 2007 country snapshot from GDP per person.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does GDP per person show a training pattern related to life expectancy?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Does adding curved terms improve the numeric prediction relationship?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Is degree 2 or degree 3 more useful?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final life expectancy error compare with the initial-model CV and any selected-setting CV for Gapminder · 1 continuous feature, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in life expectancy units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      },
      "regression_tree": {
        "dataset": "gapminder",
        "scenario": "simple",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "gdpPercap"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"gdpPercap\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Trees split on thresholds, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn if/then splits for nonlinear numeric predictions\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeRegressor\n\nmodel = DecisionTreeRegressor(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate life expectancy for the 2007 country snapshot from GDP per person.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does GDP per person show a training pattern related to life expectancy?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful numeric prediction groups?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final life expectancy error compare with the initial-model CV and any selected-setting CV for Gapminder · 1 continuous feature, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in life expectancy units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      }
    },
    "candy_simple": {
      "simple_linear": {
        "dataset": "candy",
        "scenario": "simple",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Original numeric units stay visible so linear coefficients are easier to interpret.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Fit one straight-line relationship\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LinearRegression\n\nmodel = LinearRegression()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate candy win rate from sugarpercent; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to candy win rate?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How much does one selected input move the numeric target along a straight line?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final candy win rate error compare with the initial-model CV and any selected-setting CV for Candy Power Ranking · 1 continuous feature, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in candy win rate units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      },
      "polynomial": {
        "dataset": "candy",
        "scenario": "simple",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# The model scales after polynomial expansion so Ridge regularisation treats terms comparably.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Expand continuous inputs into curved terms, then scale and regularise\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.preprocessing import PolynomialFeatures, StandardScaler\nfrom sklearn.linear_model import Ridge\n\npolynomial = PolynomialFeatures(degree=2, include_bias=False)\nscale = StandardScaler()\nmodel = Ridge()\npipeline = Pipeline([\n    (\"polynomial\", polynomial),\n    (\"scale\", scale),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'polynomial__degree': [2, 3]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate candy win rate from sugarpercent; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to candy win rate?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Does adding curved terms improve the numeric prediction relationship?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Is degree 2 or degree 3 more useful?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final candy win rate error compare with the initial-model CV and any selected-setting CV for Candy Power Ranking · 1 continuous feature, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in candy win rate units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      },
      "regression_tree": {
        "dataset": "candy",
        "scenario": "simple",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Trees split on thresholds, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn if/then splits for nonlinear numeric predictions\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeRegressor\n\nmodel = DecisionTreeRegressor(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate candy win rate from sugarpercent; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to candy win rate?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful numeric prediction groups?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final candy win rate error compare with the initial-model CV and any selected-setting CV for Candy Power Ranking · 1 continuous feature, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in candy win rate units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      }
    },
    "candy": {
      "multiple_linear": {
        "dataset": "candy",
        "scenario": "continuous_binary",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent",
              "pricepercent"
            ],
            "operation": "passthrough"
          },
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\",\"pricepercent\"]\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Original numeric units stay visible so linear coefficients are easier to interpret.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Estimate one adjusted linear effect per encoded predictor\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LinearRegression\n\nmodel = LinearRegression()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate candy win rate from 11 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to candy win rate?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "What additive contribution does each selected input make while the others are held in the model?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final candy win rate error compare with the initial-model CV and any selected-setting CV for Candy Power Ranking · Continuous + binary, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in candy win rate units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      },
      "regression_tree": {
        "dataset": "candy",
        "scenario": "continuous_binary",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent",
              "pricepercent"
            ],
            "operation": "passthrough"
          },
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\",\"pricepercent\"]\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Trees split on thresholds, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn if/then splits for nonlinear numeric predictions\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeRegressor\n\nmodel = DecisionTreeRegressor(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate candy win rate from 11 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to candy win rate?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful numeric prediction groups?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final candy win rate error compare with the initial-model CV and any selected-setting CV for Candy Power Ranking · Continuous + binary, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in candy win rate units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      }
    },
    "seoul": {
      "multiple_linear": {
        "dataset": "seoul",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "Temperature(°C)",
              "Humidity(%)",
              "Wind speed (m/s)",
              "Visibility (10m)",
              "Solar Radiation (MJ/m2)",
              "Rainfall(mm)",
              "Snowfall (cm)"
            ],
            "operation": "passthrough"
          },
          {
            "label": "Categorical features",
            "columns": [
              "Holiday",
              "Hour",
              "Seasons"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = [\"Temperature(°C)\",\"Humidity(%)\",\"Wind speed (m/s)\",\"Visibility (10m)\",\"Solar Radiation (MJ/m2)\",\"Rainfall(mm)\",\"Snowfall (cm)\"]\nbinary_features = [\"Holiday\"]\ncategorical_features = [\"Hour\",\"Seasons\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"Holiday\"]\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Original numeric units stay visible so linear coefficients are easier to interpret.\nfrom sklearn.preprocessing import OneHotEncoder\nfrom sklearn.compose import ColumnTransformer\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop='first')\npreprocessor = ColumnTransformer([\n    (\"continuous\", \"passthrough\", numeric_features),\n    (\"encoded\", category_encoder, encoded_binary_features + categorical_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Estimate one adjusted linear effect per encoded predictor\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LinearRegression\n\nmodel = LinearRegression()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict later bike-rental demand from 10 selected inputs; rows stay in chronological order.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the earlier training count and later saved-test count."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does temperature show a training pattern related to bike-rental demand?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "What additive contribution does each selected input make while the others are held in the model?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Look for whether validation scores change across later time windows; these folds are ordered rather than random."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final bike-rental demand error compare with the initial-model CV and any selected-setting CV across later time windows for Seoul Bike Sharing Demand · Continuous + binary + categorical, and which inputs would actually be available for a future prediction?",
            "readingCue": "Compare the saved final test error with initial-model CV and selected-setting CV when tuning was used, then check that any proposed future input would be known before the prediction time."
          }
        ]
      },
      "regression_tree": {
        "dataset": "seoul",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "Temperature(°C)",
              "Humidity(%)",
              "Wind speed (m/s)",
              "Visibility (10m)",
              "Solar Radiation (MJ/m2)",
              "Rainfall(mm)",
              "Snowfall (cm)"
            ],
            "operation": "passthrough"
          },
          {
            "label": "Categorical features",
            "columns": [
              "Holiday",
              "Hour",
              "Seasons"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = [\"Temperature(°C)\",\"Humidity(%)\",\"Wind speed (m/s)\",\"Visibility (10m)\",\"Solar Radiation (MJ/m2)\",\"Rainfall(mm)\",\"Snowfall (cm)\"]\nbinary_features = [\"Holiday\"]\ncategorical_features = [\"Hour\",\"Seasons\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"Holiday\"]\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Trees split on thresholds, so scaling is unnecessary.\nfrom sklearn.preprocessing import OneHotEncoder\nfrom sklearn.compose import ColumnTransformer\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = ColumnTransformer([\n    (\"continuous\", \"passthrough\", numeric_features),\n    (\"encoded\", category_encoder, encoded_binary_features + categorical_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn if/then splits for nonlinear numeric predictions\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeRegressor\n\nmodel = DecisionTreeRegressor(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict later bike-rental demand from 10 selected inputs; rows stay in chronological order.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the earlier training count and later saved-test count."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does temperature show a training pattern related to bike-rental demand?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful numeric prediction groups?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Look for whether validation scores change across later time windows; these folds are ordered rather than random."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final bike-rental demand error compare with the initial-model CV and any selected-setting CV across later time windows for Seoul Bike Sharing Demand · Continuous + binary + categorical, and which inputs would actually be available for a future prediction?",
            "readingCue": "Compare the saved final test error with initial-model CV and selected-setting CV when tuning was used, then check that any proposed future input would be known before the prediction time."
          }
        ]
      },
      "mlp_reg": {
        "dataset": "seoul",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "Temperature(°C)",
              "Humidity(%)",
              "Wind speed (m/s)",
              "Visibility (10m)",
              "Solar Radiation (MJ/m2)",
              "Rainfall(mm)",
              "Snowfall (cm)"
            ],
            "operation": "scale"
          },
          {
            "label": "Categorical features",
            "columns": [
              "Holiday",
              "Hour",
              "Seasons"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = [\"Temperature(°C)\",\"Humidity(%)\",\"Wind speed (m/s)\",\"Visibility (10m)\",\"Solar Radiation (MJ/m2)\",\"Rainfall(mm)\",\"Snowfall (cm)\"]\nbinary_features = [\"Holiday\"]\ncategorical_features = [\"Hour\",\"Seasons\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"Holiday\"]\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Comparable feature scales make neural-network optimisation easier.\nfrom sklearn.preprocessing import OneHotEncoder\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import StandardScaler\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = ColumnTransformer([\n    (\"continuous\", StandardScaler(), numeric_features),\n    (\"encoded\", category_encoder, encoded_binary_features + categorical_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn nonlinear layers while scaling the target inside the model\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neural_network import MLPRegressor\nfrom sklearn.compose import TransformedTargetRegressor\nfrom sklearn.preprocessing import StandardScaler\n\nnetwork = MLPRegressor(\n    hidden_layer_sizes=(24,),\n    max_iter=800,\n    early_stopping=False,\n    tol=1e-3,\n    random_state=42,\n)\nmodel = TransformedTargetRegressor(\n    regressor=network,\n    transformer=StandardScaler(),\n)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__regressor__hidden_layer_sizes': [(16,), (24,)]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict later bike-rental demand from 10 selected inputs; rows stay in chronological order.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the earlier training count and later saved-test count."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does temperature show a training pattern related to bike-rental demand?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can one small hidden layer learn nonlinear numeric patterns?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Look for whether validation scores change across later time windows; these folds are ordered rather than random."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does a different small hidden-layer size help?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final bike-rental demand error compare with the initial-model CV and any selected-setting CV across later time windows for Seoul Bike Sharing Demand · Continuous + binary + categorical, and which inputs would actually be available for a future prediction?",
            "readingCue": "Compare the saved final test error with initial-model CV and selected-setting CV when tuning was used, then check that any proposed future input would be known before the prediction time."
          }
        ]
      }
    },
    "penguins_mixed": {
      "logistic": {
        "dataset": "penguins",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "scale"
          },
          {
            "label": "Categorical features",
            "columns": [
              "sex",
              "island",
              "year"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = [\"sex\"]\ncategorical_features = [\"island\",\"year\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"sex\"]\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Scaling helps optimisation and makes regularisation act more evenly across features.\nfrom sklearn.preprocessing import OneHotEncoder\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import StandardScaler\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = ColumnTransformer([\n    (\"continuous\", StandardScaler(), numeric_features),\n    (\"encoded\", category_encoder, encoded_binary_features + categorical_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Model class log-odds with a regularised linear boundary\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LogisticRegression\n\nmodel = LogisticRegression(max_iter=2000, random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.1, 1.0, 10.0]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 7 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can a regularised linear score separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does regularisation strength help the class boundary?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · Continuous + binary + categorical context? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "svm_cls": {
        "dataset": "penguins",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "scale"
          },
          {
            "label": "Categorical features",
            "columns": [
              "sex",
              "island",
              "year"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = [\"sex\"]\ncategorical_features = [\"island\",\"year\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"sex\"]\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Scaling prevents large-unit features from dominating the boundary and kernel.\nfrom sklearn.preprocessing import OneHotEncoder\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import StandardScaler\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = ColumnTransformer([\n    (\"continuous\", StandardScaler(), numeric_features),\n    (\"encoded\", category_encoder, encoded_binary_features + categorical_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Find a maximum-margin boundary; RBF allows curvature\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.svm import SVC\n\nmodel = SVC(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.5, 2, 10]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 7 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How does a margin based boundary separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How strongly should classification mistakes be penalised?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · Continuous + binary + categorical context? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "one_r": {
        "dataset": "penguins",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "passthrough"
          },
          {
            "label": "Categorical features",
            "columns": [
              "sex",
              "island",
              "year"
            ],
            "operation": "use one stable category code"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = [\"sex\"]\ncategorical_features = [\"island\",\"year\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"sex\"]\n# 4 · Define the One-R preparation recipe\n# The public adapter keeps feature types and fits category codes inside each CV fold.\npreprocessor = OneRPreprocessor(\n    numeric_features=numeric_features,\n    categorical_features=binary_features + categorical_features,\n)\npreprocessor\n# 5 · Build the model pipeline\n# Use the single feature whose simple rules make the fewest errors\n\nfrom sklearn.pipeline import Pipeline\n\n# One-R and its public feature-type adapter are preloaded helpers.\n\nmodel = OneRClassifier(bins=5)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__bins': [3, 5, 8]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 7 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while each original category stays a separate One-R value."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which single feature gives the simplest useful class rules?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many numeric intervals are useful for the one-feature rule?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · Continuous + binary + categorical context? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "classification_tree": {
        "dataset": "penguins",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "passthrough"
          },
          {
            "label": "Categorical features",
            "columns": [
              "sex",
              "island",
              "year"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = [\"sex\"]\ncategorical_features = [\"island\",\"year\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"sex\"]\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Trees split on thresholds, so scaling is unnecessary.\nfrom sklearn.preprocessing import OneHotEncoder\nfrom sklearn.compose import ColumnTransformer\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = ColumnTransformer([\n    (\"continuous\", \"passthrough\", numeric_features),\n    (\"encoded\", category_encoder, encoded_binary_features + categorical_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn interpretable if/then splits for class labels\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeClassifier\n\nmodel = DecisionTreeClassifier(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 7 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful class decisions?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · Continuous + binary + categorical context? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "knn_cls": {
        "dataset": "penguins",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "scale"
          },
          {
            "label": "Categorical features",
            "columns": [
              "sex",
              "island",
              "year"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = [\"sex\"]\ncategorical_features = [\"island\",\"year\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"sex\"]\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Scaling matters because neighbour selection is based on distance.\nfrom sklearn.preprocessing import OneHotEncoder\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import StandardScaler\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = ColumnTransformer([\n    (\"continuous\", StandardScaler(), numeric_features),\n    (\"encoded\", category_encoder, encoded_binary_features + categorical_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Vote using nearby training examples; distance makes scaling essential\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neighbors import KNeighborsClassifier\n\nmodel = KNeighborsClassifier()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__n_neighbors': [3, 5, 9]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 7 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Do nearby prepared training rows vote for the same class?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many neighbours should vote?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · Continuous + binary + categorical context? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "mlp_cls": {
        "dataset": "penguins",
        "scenario": "all_types",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "scale"
          },
          {
            "label": "Categorical features",
            "columns": [
              "sex",
              "island",
              "year"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = [\"sex\"]\ncategorical_features = [\"island\",\"year\"]\nnumeric_binary_features = []\nencoded_binary_features = [\"sex\"]\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Comparable feature scales make neural-network optimisation easier.\nfrom sklearn.preprocessing import OneHotEncoder\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import StandardScaler\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = ColumnTransformer([\n    (\"continuous\", StandardScaler(), numeric_features),\n    (\"encoded\", category_encoder, encoded_binary_features + categorical_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn nonlinear layers of weighted features with backpropagation\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neural_network import MLPClassifier\n\nmodel = MLPClassifier(\n    hidden_layer_sizes=(24,),\n    max_iter=500,\n    early_stopping=True,\n    random_state=42\n)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__hidden_layer_sizes': [(16,), (24,)]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 7 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can one small hidden layer learn nonlinear class patterns?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does a different small hidden-layer size help?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · Continuous + binary + categorical context? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      }
    },
    "penguins": {
      "logistic": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling helps optimisation and makes regularisation act more evenly across features.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Model class log-odds with a regularised linear boundary\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LogisticRegression\n\nmodel = LogisticRegression(max_iter=2000, random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.1, 1.0, 10.0]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can a regularised linear score separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does regularisation strength help the class boundary?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "svm_cls": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling prevents large-unit features from dominating the boundary and kernel.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Find a maximum-margin boundary; RBF allows curvature\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.svm import SVC\n\nmodel = SVC(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.5, 2, 10]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How does a margin based boundary separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How strongly should classification mistakes be penalised?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "one_r": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Define the One-R preparation recipe\n# The public adapter keeps feature types and fits category codes inside each CV fold.\npreprocessor = OneRPreprocessor(\n    numeric_features=numeric_features,\n    categorical_features=[],\n)\npreprocessor\n# 5 · Build the model pipeline\n# Use the single feature whose simple rules make the fewest errors\n\nfrom sklearn.pipeline import Pipeline\n\n# One-R and its public feature-type adapter are preloaded helpers.\n\nmodel = OneRClassifier(bins=5)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__bins': [3, 5, 8]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which single feature gives the simplest useful class rules?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many numeric intervals are useful for the one-feature rule?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "classification_tree": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Trees split on thresholds, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn interpretable if/then splits for class labels\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeClassifier\n\nmodel = DecisionTreeClassifier(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful class decisions?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "knn_cls": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling matters because neighbour selection is based on distance.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Vote using nearby training examples; distance makes scaling essential\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neighbors import KNeighborsClassifier\n\nmodel = KNeighborsClassifier()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__n_neighbors': [3, 5, 9]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Do nearby prepared training rows vote for the same class?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many neighbours should vote?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "qda": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling is not required here because each class estimates its own feature relationships.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Give each class its own covariance shape and curved boundary\n# A small amount of regularisation keeps class covariance estimates stable.\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.discriminant_analysis import QuadraticDiscriminantAnalysis\n\nmodel = QuadraticDiscriminantAnalysis(reg_param=0.1)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__reg_param': [0.1, 0.2, 0.5, 0.9]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Do class specific shapes produce a useful curved boundary?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How much should class-shape estimates be stabilised?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "lda": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling is not required here because the class boundaries use each feature's estimated relationships.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Share one covariance shape and learn linear class boundaries\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.discriminant_analysis import LinearDiscriminantAnalysis\n\nmodel = LinearDiscriminantAnalysis(solver='lsqr')\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__shrinkage': [None, 'auto']\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can one shared class shape support a useful linear boundary?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does stabilising the shared class shape help?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "naive_bayes": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# This Naive Bayes family models class-conditional feature evidence/distributions directly, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Estimate class-conditional feature densities and class probabilities with a Gaussian distribution per feature\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.naive_bayes import GaussianNB\n\nmodel = GaussianNB()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__var_smoothing': [1e-11, 1e-9, 1e-7]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How do class priors and feature evidence combine under the independence assumption?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How much smoothing should class evidence use?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "mlp_cls": {
        "dataset": "penguins",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "bill_length_mm",
              "bill_depth_mm",
              "flipper_length_mm",
              "body_mass_g"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"bill_length_mm\",\"bill_depth_mm\",\"flipper_length_mm\",\"body_mass_g\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Comparable feature scales make neural-network optimisation easier.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn nonlinear layers of weighted features with backpropagation\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neural_network import MLPClassifier\n\nmodel = MLPClassifier(\n    hidden_layer_sizes=(24,),\n    max_iter=500,\n    early_stopping=True,\n    random_state=42\n)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__hidden_layer_sizes': [(16,), (24,)]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict penguin species from 4 selected inputs; each row is one penguin.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does bill_length_mm show a training pattern related to penguin species?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can one small hidden layer learn nonlinear class patterns?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does a different small hidden-layer size help?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final penguin species result compare with the initial-model CV and any selected-setting CV for Palmer Penguins · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final penguin species metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      }
    },
    "car": {
      "logistic": {
        "dataset": "car",
        "scenario": "categorical",
        "preparation": [
          {
            "label": "Categorical features",
            "columns": [
              "buying",
              "maintenance",
              "doors",
              "persons",
              "luggage_boot",
              "safety"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = []\nbinary_features = []\ncategorical_features = [\"buying\",\"maintenance\",\"doors\",\"persons\",\"luggage_boot\",\"safety\"]\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Scaling helps optimisation and makes regularisation act more evenly across features.\nfrom sklearn.preprocessing import OneHotEncoder\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = category_encoder\n\npreprocessor\n# 5 · Build the model pipeline\n# Model class log-odds with a regularised linear boundary\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LogisticRegression\n\nmodel = LogisticRegression(max_iter=2000, random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.1, 1.0, 10.0]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "What changes when every predictor is categorical? Use 6 selected inputs to estimate car acceptability.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does buying show a training pattern related to car acceptability?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can a regularised linear score separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does regularisation strength help the class boundary?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final car acceptability result compare with the initial-model CV and any selected-setting CV for Car Evaluation · All features categorical? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final car acceptability metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "svm_cls": {
        "dataset": "car",
        "scenario": "categorical",
        "preparation": [
          {
            "label": "Categorical features",
            "columns": [
              "buying",
              "maintenance",
              "doors",
              "persons",
              "luggage_boot",
              "safety"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = []\nbinary_features = []\ncategorical_features = [\"buying\",\"maintenance\",\"doors\",\"persons\",\"luggage_boot\",\"safety\"]\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Scaling prevents large-unit features from dominating the boundary and kernel.\nfrom sklearn.preprocessing import OneHotEncoder\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = category_encoder\n\npreprocessor\n# 5 · Build the model pipeline\n# Find a maximum-margin boundary; RBF allows curvature\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.svm import SVC\n\nmodel = SVC(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.5, 2, 10]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "What changes when every predictor is categorical? Use 6 selected inputs to estimate car acceptability.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does buying show a training pattern related to car acceptability?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How does a margin based boundary separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How strongly should classification mistakes be penalised?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final car acceptability result compare with the initial-model CV and any selected-setting CV for Car Evaluation · All features categorical? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final car acceptability metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "one_r": {
        "dataset": "car",
        "scenario": "categorical",
        "preparation": [
          {
            "label": "Categorical features",
            "columns": [
              "buying",
              "maintenance",
              "doors",
              "persons",
              "luggage_boot",
              "safety"
            ],
            "operation": "use one stable category code"
          }
        ],
        "code": "numeric_features = []\nbinary_features = []\ncategorical_features = [\"buying\",\"maintenance\",\"doors\",\"persons\",\"luggage_boot\",\"safety\"]\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Define the One-R preparation recipe\n# The public adapter keeps feature types and fits category codes inside each CV fold.\npreprocessor = OneRPreprocessor(\n    numeric_features=[],\n    categorical_features=categorical_features,\n)\npreprocessor\n# 5 · Build the model pipeline\n# Use the single feature whose simple rules make the fewest errors\n\nfrom sklearn.pipeline import Pipeline\n\n# One-R and its public feature-type adapter are preloaded helpers.\n\nmodel = OneRClassifier(bins=5)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "What changes when every predictor is categorical? Use 6 selected inputs to estimate car acceptability.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does buying show a training pattern related to car acceptability?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while each original category stays a separate One-R value."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which single feature gives the simplest useful class rules?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final car acceptability result compare with the initial-model CV and any selected-setting CV for Car Evaluation · All features categorical? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final car acceptability metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "classification_tree": {
        "dataset": "car",
        "scenario": "categorical",
        "preparation": [
          {
            "label": "Categorical features",
            "columns": [
              "buying",
              "maintenance",
              "doors",
              "persons",
              "luggage_boot",
              "safety"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = []\nbinary_features = []\ncategorical_features = [\"buying\",\"maintenance\",\"doors\",\"persons\",\"luggage_boot\",\"safety\"]\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Trees split on thresholds, so scaling is unnecessary.\nfrom sklearn.preprocessing import OneHotEncoder\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = category_encoder\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn interpretable if/then splits for class labels\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeClassifier\n\nmodel = DecisionTreeClassifier(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "What changes when every predictor is categorical? Use 6 selected inputs to estimate car acceptability.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does buying show a training pattern related to car acceptability?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful class decisions?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final car acceptability result compare with the initial-model CV and any selected-setting CV for Car Evaluation · All features categorical? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final car acceptability metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "knn_cls": {
        "dataset": "car",
        "scenario": "categorical",
        "preparation": [
          {
            "label": "Categorical features",
            "columns": [
              "buying",
              "maintenance",
              "doors",
              "persons",
              "luggage_boot",
              "safety"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = []\nbinary_features = []\ncategorical_features = [\"buying\",\"maintenance\",\"doors\",\"persons\",\"luggage_boot\",\"safety\"]\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Scaling matters because neighbour selection is based on distance.\nfrom sklearn.preprocessing import OneHotEncoder\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = category_encoder\n\npreprocessor\n# 5 · Build the model pipeline\n# Vote using nearby training examples; distance makes scaling essential\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neighbors import KNeighborsClassifier\n\nmodel = KNeighborsClassifier()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__n_neighbors': [3, 5, 9]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "What changes when every predictor is categorical? Use 6 selected inputs to estimate car acceptability.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does buying show a training pattern related to car acceptability?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Do nearby prepared training rows vote for the same class?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many neighbours should vote?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final car acceptability result compare with the initial-model CV and any selected-setting CV for Car Evaluation · All features categorical? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final car acceptability metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "naive_bayes": {
        "dataset": "car",
        "scenario": "categorical",
        "preparation": [
          {
            "label": "Categorical features",
            "columns": [
              "buying",
              "maintenance",
              "doors",
              "persons",
              "luggage_boot",
              "safety"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = []\nbinary_features = []\ncategorical_features = [\"buying\",\"maintenance\",\"doors\",\"persons\",\"luggage_boot\",\"safety\"]\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Each category becomes a yes/no feature, so Bernoulli Naive Bayes can learn category likelihoods safely.\n# This Naive Bayes family models class-conditional feature evidence/distributions directly, so scaling is unnecessary.\nfrom sklearn.preprocessing import OneHotEncoder\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = category_encoder\n\npreprocessor\n# 5 · Build the model pipeline\n# Turn categories into yes/no features and estimate their likelihood within each class\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.naive_bayes import BernoulliNB\n\nmodel = BernoulliNB()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__alpha': [0.1, 1.0, 5.0]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "What changes when every predictor is categorical? Use 6 selected inputs to estimate car acceptability.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does buying show a training pattern related to car acceptability?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How do class priors and feature evidence combine under the independence assumption?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How much smoothing should class evidence use?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final car acceptability result compare with the initial-model CV and any selected-setting CV for Car Evaluation · All features categorical? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final car acceptability metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "mlp_cls": {
        "dataset": "car",
        "scenario": "categorical",
        "preparation": [
          {
            "label": "Categorical features",
            "columns": [
              "buying",
              "maintenance",
              "doors",
              "persons",
              "luggage_boot",
              "safety"
            ],
            "operation": "one-hot encode"
          }
        ],
        "code": "numeric_features = []\nbinary_features = []\ncategorical_features = [\"buying\",\"maintenance\",\"doors\",\"persons\",\"luggage_boot\",\"safety\"]\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Categories are one-hot encoded so the estimator receives numeric inputs.\n# Comparable feature scales make neural-network optimisation easier.\nfrom sklearn.preprocessing import OneHotEncoder\n\ncategory_encoder = OneHotEncoder(handle_unknown=\"ignore\", sparse_output=False, drop=None)\npreprocessor = category_encoder\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn nonlinear layers of weighted features with backpropagation\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neural_network import MLPClassifier\n\nmodel = MLPClassifier(\n    hidden_layer_sizes=(24,),\n    max_iter=500,\n    early_stopping=True,\n    random_state=42\n)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__hidden_layer_sizes': [(16,), (24,)]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "What changes when every predictor is categorical? Use 6 selected inputs to estimate car acceptability.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does buying show a training pattern related to car acceptability?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can one small hidden layer learn nonlinear class patterns?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does a different small hidden-layer size help?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final car acceptability result compare with the initial-model CV and any selected-setting CV for Car Evaluation · All features categorical? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final car acceptability metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      }
    },
    "breast": {
      "logistic": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling helps optimisation and makes regularisation act more evenly across features.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Model class log-odds with a regularised linear boundary\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LogisticRegression\n\nmodel = LogisticRegression(max_iter=2000, random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.1, 1.0, 10.0]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can a regularised linear score separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does regularisation strength help the class boundary?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "svm_cls": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling prevents large-unit features from dominating the boundary and kernel.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Find a maximum-margin boundary; RBF allows curvature\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.svm import SVC\n\nmodel = SVC(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.5, 2, 10]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How does a margin based boundary separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How strongly should classification mistakes be penalised?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "one_r": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Define the One-R preparation recipe\n# The public adapter keeps feature types and fits category codes inside each CV fold.\npreprocessor = OneRPreprocessor(\n    numeric_features=numeric_features,\n    categorical_features=[],\n)\npreprocessor\n# 5 · Build the model pipeline\n# Use the single feature whose simple rules make the fewest errors\n\nfrom sklearn.pipeline import Pipeline\n\n# One-R and its public feature-type adapter are preloaded helpers.\n\nmodel = OneRClassifier(bins=5)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__bins': [3, 5, 8]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which single feature gives the simplest useful class rules?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many numeric intervals are useful for the one-feature rule?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "classification_tree": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Trees split on thresholds, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn interpretable if/then splits for class labels\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeClassifier\n\nmodel = DecisionTreeClassifier(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful class decisions?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "knn_cls": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling matters because neighbour selection is based on distance.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Vote using nearby training examples; distance makes scaling essential\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neighbors import KNeighborsClassifier\n\nmodel = KNeighborsClassifier()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__n_neighbors': [3, 5, 9]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Do nearby prepared training rows vote for the same class?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many neighbours should vote?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "qda": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling is not required here because each class estimates its own feature relationships.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Give each class its own covariance shape and curved boundary\n# A small amount of regularisation keeps class covariance estimates stable.\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.discriminant_analysis import QuadraticDiscriminantAnalysis\n\nmodel = QuadraticDiscriminantAnalysis(reg_param=0.1)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__reg_param': [0.1, 0.2, 0.5, 0.9]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Do class specific shapes produce a useful curved boundary?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How much should class-shape estimates be stabilised?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "lda": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling is not required here because the class boundaries use each feature's estimated relationships.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Share one covariance shape and learn linear class boundaries\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.discriminant_analysis import LinearDiscriminantAnalysis\n\nmodel = LinearDiscriminantAnalysis(solver='lsqr')\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__shrinkage': [None, 'auto']\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can one shared class shape support a useful linear boundary?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does stabilising the shared class shape help?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "naive_bayes": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# This Naive Bayes family models class-conditional feature evidence/distributions directly, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Estimate class-conditional feature densities and class probabilities with a Gaussian distribution per feature\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.naive_bayes import GaussianNB\n\nmodel = GaussianNB()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__var_smoothing': [1e-11, 1e-9, 1e-7]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How do class priors and feature evidence combine under the independence assumption?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How much smoothing should class evidence use?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "mlp_cls": {
        "dataset": "breast",
        "scenario": "continuous5",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "radius_mean",
              "texture_mean",
              "smoothness_mean",
              "concavity_mean",
              "symmetry_mean"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"radius_mean\",\"texture_mean\",\"smoothness_mean\",\"concavity_mean\",\"symmetry_mean\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Comparable feature scales make neural-network optimisation easier.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn nonlinear layers of weighted features with backpropagation\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neural_network import MLPClassifier\n\nmodel = MLPClassifier(\n    hidden_layer_sizes=(24,),\n    max_iter=500,\n    early_stopping=True,\n    random_state=42\n)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__hidden_layer_sizes': [(16,), (24,)]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Predict diagnosis class from 5 selected inputs; each row is one tumour sample.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does radius_mean show a training pattern related to diagnosis class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can one small hidden layer learn nonlinear class patterns?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does a different small hidden-layer size help?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final diagnosis class result compare with the initial-model CV and any selected-setting CV for Breast Cancer Wisconsin (Diagnostic) · All features continuous? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final diagnosis class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      }
    },
    "candy_binary": {
      "logistic": {
        "dataset": "candy_class",
        "scenario": "binary",
        "preparation": [
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = []\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling helps optimisation and makes regularisation act more evenly across features.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Model class log-odds with a regularised linear boundary\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LogisticRegression\n\nmodel = LogisticRegression(max_iter=2000, random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.1, 1.0, 10.0]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 9 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does chocolate show a training pattern related to popularity class?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can a regularised linear score separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does regularisation strength help the class boundary?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · All features binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "svm_cls": {
        "dataset": "candy_class",
        "scenario": "binary",
        "preparation": [
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = []\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling prevents large-unit features from dominating the boundary and kernel.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Find a maximum-margin boundary; RBF allows curvature\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.svm import SVC\n\nmodel = SVC(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.5, 2, 10]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 9 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does chocolate show a training pattern related to popularity class?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How does a margin based boundary separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How strongly should classification mistakes be penalised?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · All features binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "one_r": {
        "dataset": "candy_class",
        "scenario": "binary",
        "preparation": [
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = []\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Define the One-R preparation recipe\n# The public adapter keeps feature types and fits category codes inside each CV fold.\npreprocessor = OneRPreprocessor(\n    numeric_features=[],\n    categorical_features=binary_features,\n)\npreprocessor\n# 5 · Build the model pipeline\n# Use the single feature whose simple rules make the fewest errors\n\nfrom sklearn.pipeline import Pipeline\n\n# One-R and its public feature-type adapter are preloaded helpers.\n\nmodel = OneRClassifier(bins=5)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 9 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does chocolate show a training pattern related to popularity class?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while each original category stays a separate One-R value."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which single feature gives the simplest useful class rules?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · All features binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "classification_tree": {
        "dataset": "candy_class",
        "scenario": "binary",
        "preparation": [
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = []\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Trees split on thresholds, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn interpretable if/then splits for class labels\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeClassifier\n\nmodel = DecisionTreeClassifier(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 9 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does chocolate show a training pattern related to popularity class?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful class decisions?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · All features binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "knn_cls": {
        "dataset": "candy_class",
        "scenario": "binary",
        "preparation": [
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = []\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling matters because neighbour selection is based on distance.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Vote using nearby training examples; distance makes scaling essential\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neighbors import KNeighborsClassifier\n\nmodel = KNeighborsClassifier()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__n_neighbors': [3, 5, 9]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 9 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does chocolate show a training pattern related to popularity class?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Do nearby prepared training rows vote for the same class?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many neighbours should vote?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · All features binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "naive_bayes": {
        "dataset": "candy_class",
        "scenario": "binary",
        "preparation": [
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = []\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# This Naive Bayes family models class-conditional feature evidence/distributions directly, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Estimate independent Bernoulli probabilities for binary inputs\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.naive_bayes import BernoulliNB\n\nmodel = BernoulliNB()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__alpha': [0.1, 1.0, 5.0]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 9 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does chocolate show a training pattern related to popularity class?",
            "readingCue": "Look for groups whose class mixes differ, while remembering that overlap can remain."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How do class priors and feature evidence combine under the independence assumption?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How much smoothing should class evidence use?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · All features binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      }
    },
    "candy_mixed": {
      "logistic": {
        "dataset": "candy_class",
        "scenario": "continuous_binary",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent",
              "pricepercent"
            ],
            "operation": "scale"
          },
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\",\"pricepercent\"]\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling helps optimisation and makes regularisation act more evenly across features.\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = ColumnTransformer([\n    (\"continuous\", StandardScaler(), numeric_features),\n    (\"numeric_binary\", \"passthrough\", numeric_binary_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Model class log-odds with a regularised linear boundary\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LogisticRegression\n\nmodel = LogisticRegression(max_iter=2000, random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.1, 1.0, 10.0]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 11 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to popularity class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can a regularised linear score separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does regularisation strength help the class boundary?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · Continuous + binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "svm_cls": {
        "dataset": "candy_class",
        "scenario": "continuous_binary",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent",
              "pricepercent"
            ],
            "operation": "scale"
          },
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\",\"pricepercent\"]\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling prevents large-unit features from dominating the boundary and kernel.\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = ColumnTransformer([\n    (\"continuous\", StandardScaler(), numeric_features),\n    (\"numeric_binary\", \"passthrough\", numeric_binary_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Find a maximum-margin boundary; RBF allows curvature\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.svm import SVC\n\nmodel = SVC(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__C': [0.5, 2, 10]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 11 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to popularity class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "How does a margin based boundary separate the classes?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How strongly should classification mistakes be penalised?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · Continuous + binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "one_r": {
        "dataset": "candy_class",
        "scenario": "continuous_binary",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent",
              "pricepercent"
            ],
            "operation": "passthrough"
          },
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\",\"pricepercent\"]\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Define the One-R preparation recipe\n# The public adapter keeps feature types and fits category codes inside each CV fold.\npreprocessor = OneRPreprocessor(\n    numeric_features=numeric_features,\n    categorical_features=binary_features,\n)\npreprocessor\n# 5 · Build the model pipeline\n# Use the single feature whose simple rules make the fewest errors\n\nfrom sklearn.pipeline import Pipeline\n\n# One-R and its public feature-type adapter are preloaded helpers.\n\nmodel = OneRClassifier(bins=5)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__bins': [3, 5, 8]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 11 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to popularity class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while each original category stays a separate One-R value."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which single feature gives the simplest useful class rules?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many numeric intervals are useful for the one-feature rule?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · Continuous + binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "classification_tree": {
        "dataset": "candy_class",
        "scenario": "continuous_binary",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent",
              "pricepercent"
            ],
            "operation": "passthrough"
          },
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\",\"pricepercent\"]\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Trees split on thresholds, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn interpretable if/then splits for class labels\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeClassifier\n\nmodel = DecisionTreeClassifier(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 11 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to popularity class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded while continuous features keep their numeric role inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful class decisions?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · Continuous + binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      },
      "knn_cls": {
        "dataset": "candy_class",
        "scenario": "continuous_binary",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "sugarpercent",
              "pricepercent"
            ],
            "operation": "scale"
          },
          {
            "label": "Binary indicators",
            "columns": [
              "chocolate",
              "fruity",
              "caramel",
              "peanutyalmondy",
              "nougat",
              "crispedricewafer",
              "hard",
              "bar",
              "pluribus"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"sugarpercent\",\"pricepercent\"]\nbinary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\ncategorical_features = []\nnumeric_binary_features = [\"chocolate\",\"fruity\",\"caramel\",\"peanutyalmondy\",\"nougat\",\"crispedricewafer\",\"hard\",\"bar\",\"pluribus\"]\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Scaling matters because neighbour selection is based on distance.\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = ColumnTransformer([\n    (\"continuous\", StandardScaler(), numeric_features),\n    (\"numeric_binary\", \"passthrough\", numeric_binary_features)\n], verbose_feature_names_out=False)\n\npreprocessor\n# 5 · Build the model pipeline\n# Vote using nearby training examples; distance makes scaling essential\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neighbors import KNeighborsClassifier\n\nmodel = KNeighborsClassifier()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__n_neighbors': [3, 5, 9]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Classify whether a candy reaches at least 50% wins using 11 selected inputs; each row is one candy.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does sugarpercent show a training pattern related to popularity class?",
            "readingCue": "Look for how much the class distributions overlap."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for categorical features being encoded and numeric features scaled inside the pipeline."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Do nearby prepared training rows vote for the same class?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean macro F1 with the most-common-class reference; higher is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How many neighbours should vote?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Which classes are most often confused in training-only validation?",
            "readingCue": "Look for which actual classes are most often confused and whether errors cluster by class."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the selected model's saved final popularity class result compare with the initial-model CV and any selected-setting CV for Candy Popularity · Continuous + binary? Use the training-only diagnostic or optional final confusion view to identify a class pattern, then name one limitation.",
            "readingCue": "Read the aggregate final popularity class metric, compare it with the initial-model CV and selected-setting CV when tuning was used, then use the training-only diagnostic or optional final confusion view for class-level patterns. The saved final test is a one-time held-out check, not a tuning result."
          }
        ]
      }
    },
    "Wine600": {
      "multiple_linear": {
        "dataset": "wine",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "fixed acidity",
              "volatile acidity",
              "citric acid",
              "residual sugar",
              "chlorides",
              "free sulfur dioxide",
              "total sulfur dioxide",
              "density",
              "pH",
              "sulphates",
              "alcohol"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"fixed acidity\",\"volatile acidity\",\"citric acid\",\"residual sugar\",\"chlorides\",\"free sulfur dioxide\",\"total sulfur dioxide\",\"density\",\"pH\",\"sulphates\",\"alcohol\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Original numeric units stay visible so linear coefficients are easier to interpret.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Estimate one adjusted linear effect per encoded predictor\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import LinearRegression\n\nmodel = LinearRegression()\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate wine quality from 11 selected inputs; duplicate chemistry rows are removed before the split.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does fixed acidity show a training pattern related to wine quality?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "What additive contribution does each selected input make while the others are held in the model?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final wine quality error compare with the initial-model CV and any selected-setting CV for Wine Quality · Multiple continuous features, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in wine quality units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      },
      "polynomial": {
        "dataset": "wine",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "fixed acidity",
              "volatile acidity",
              "citric acid",
              "residual sugar",
              "chlorides",
              "free sulfur dioxide",
              "total sulfur dioxide",
              "density",
              "pH",
              "sulphates",
              "alcohol"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"fixed acidity\",\"volatile acidity\",\"citric acid\",\"residual sugar\",\"chlorides\",\"free sulfur dioxide\",\"total sulfur dioxide\",\"density\",\"pH\",\"sulphates\",\"alcohol\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# The model scales after polynomial expansion so Ridge regularisation treats terms comparably.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Expand continuous inputs into curved terms, then scale and regularise\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.preprocessing import PolynomialFeatures, StandardScaler\nfrom sklearn.linear_model import Ridge\n\npolynomial = PolynomialFeatures(degree=2, include_bias=False)\nscale = StandardScaler()\nmodel = Ridge()\npipeline = Pipeline([\n    (\"polynomial\", polynomial),\n    (\"scale\", scale),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'polynomial__degree': [2, 3]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate wine quality from 11 selected inputs; duplicate chemistry rows are removed before the split.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does fixed acidity show a training pattern related to wine quality?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Does adding curved terms improve the numeric prediction relationship?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Is degree 2 or degree 3 more useful?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final wine quality error compare with the initial-model CV and any selected-setting CV for Wine Quality · Multiple continuous features, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in wine quality units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      },
      "regression_tree": {
        "dataset": "wine",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "fixed acidity",
              "volatile acidity",
              "citric acid",
              "residual sugar",
              "chlorides",
              "free sulfur dioxide",
              "total sulfur dioxide",
              "density",
              "pH",
              "sulphates",
              "alcohol"
            ],
            "operation": "passthrough"
          }
        ],
        "code": "numeric_features = [\"fixed acidity\",\"volatile acidity\",\"citric acid\",\"residual sugar\",\"chlorides\",\"free sulfur dioxide\",\"total sulfur dioxide\",\"density\",\"pH\",\"sulphates\",\"alcohol\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Trees split on thresholds, so scaling is unnecessary.\n\npreprocessor = \"passthrough\"\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn if/then splits for nonlinear numeric predictions\n\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.tree import DecisionTreeRegressor\n\nmodel = DecisionTreeRegressor(random_state=42)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__max_depth': [3, 5, None]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate wine quality from 11 selected inputs; duplicate chemistry rows are removed before the split.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does fixed acidity show a training pattern related to wine quality?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for a pass-through or model-appropriate numeric preparation without unnecessary transformation."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Which if/then splits form useful numeric prediction groups?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "How deep should the tree grow?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final wine quality error compare with the initial-model CV and any selected-setting CV for Wine Quality · Multiple continuous features, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in wine quality units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      },
      "mlp_reg": {
        "dataset": "wine",
        "scenario": "continuous",
        "preparation": [
          {
            "label": "Numeric measurements",
            "columns": [
              "fixed acidity",
              "volatile acidity",
              "citric acid",
              "residual sugar",
              "chlorides",
              "free sulfur dioxide",
              "total sulfur dioxide",
              "density",
              "pH",
              "sulphates",
              "alcohol"
            ],
            "operation": "scale"
          }
        ],
        "code": "numeric_features = [\"fixed acidity\",\"volatile acidity\",\"citric acid\",\"residual sugar\",\"chlorides\",\"free sulfur dioxide\",\"total sulfur dioxide\",\"density\",\"pH\",\"sulphates\",\"alcohol\"]\nbinary_features = []\ncategorical_features = []\nnumeric_binary_features = []\nencoded_binary_features = []\n# 4 · Prepare the selected data\n# Keep preprocessing inside the pipeline so each CV training fold learns it from its own rows.\n# Comparable feature scales make neural-network optimisation easier.\nfrom sklearn.preprocessing import StandardScaler\n\npreprocessor = StandardScaler()\n\npreprocessor\n# 5 · Build the model pipeline\n# Learn nonlinear layers while scaling the target inside the model\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.neural_network import MLPRegressor\nfrom sklearn.compose import TransformedTargetRegressor\nfrom sklearn.preprocessing import StandardScaler\n\nnetwork = MLPRegressor(\n    hidden_layer_sizes=(24,),\n    max_iter=800,\n    early_stopping=True,\n    tol=1e-3,\n    random_state=42,\n)\nmodel = TransformedTargetRegressor(\n    regressor=network,\n    transformer=StandardScaler(),\n)\npipeline = Pipeline([\n    (\"prepare\", preprocessor),\n    (\"model\", model)\n])\npipeline\nmodel = pipeline\n",
        "grid": "{\n    'model__regressor__hidden_layer_sizes': [(16,), (24,)]\n}",
        "steps": [
          {
            "id": "frame",
            "title": "Choose what to predict",
            "question": "Estimate wine quality from 11 selected inputs; duplicate chemistry rows are removed before the split.",
            "readingCue": "Check that X contains the selected features and y contains the target."
          },
          {
            "id": "split",
            "title": "Split data and save the test set",
            "question": "Which rows are available for learning, and which are being saved for the final check?",
            "readingCue": "Check the development count and saved-test count; the saved outcomes stay sealed."
          },
          {
            "id": "explore",
            "title": "Explore training data",
            "question": "Does fixed acidity show a training pattern related to wine quality?",
            "readingCue": "Look for direction, curvature, unusual points, and how widely the target varies."
          },
          {
            "id": "prepare",
            "title": "Define the preparation recipe",
            "question": "What preparation does this particular model need?",
            "readingCue": "Look for the numeric features being scaled before this model fits."
          },
          {
            "id": "model",
            "title": "Build the model pipeline",
            "question": "Can one small hidden layer learn nonlinear numeric patterns?",
            "readingCue": "Read the model recipe, then connect its named operation to the selected inputs."
          },
          {
            "id": "baseline",
            "title": "Validate the initial model",
            "question": "Do validation results stay reasonably similar across folds?",
            "readingCue": "Read the one primary validation metric across folds; the next step compares it with a simple guess."
          },
          {
            "id": "reference",
            "title": "Compare with a simple reference",
            "question": "Do the selected features beat a simple guess on the same training folds?",
            "readingCue": "Compare the initial model's mean RMSE with the mean-target reference; lower is better."
          },
          {
            "id": "tune",
            "title": "Tune the model",
            "question": "Does a different small hidden-layer size help?",
            "readingCue": "Compare the supported setting using training folds only; the final test remains untouched."
          },
          {
            "id": "diagnose",
            "title": "Inspect validation errors",
            "question": "Do training-only residuals form a roughly random cloud around zero?",
            "readingCue": "Look for whether residuals form a roughly random cloud around zero or show a pattern."
          },
          {
            "id": "final",
            "title": "Final test",
            "question": "How does the saved final wine quality error compare with the initial-model CV and any selected-setting CV for Wine Quality · Multiple continuous features, and what pattern did the model miss?",
            "readingCue": "Read the saved final test error in wine quality units, compare it with initial-model CV and selected-setting CV when tuning was used, then name one pattern from the training-only residuals that the model did not capture."
          }
        ]
      }
    }
  },
  "discovery": {
    "kmeans": {
      "dataset": "penguins",
      "scenario": "continuous",
      "steps": [
        {
          "id": "frame",
          "title": "Choose what to discover",
          "question": "What groups can I describe in Palmer Penguins · All features continuous rows using bill_length_mm, bill_depth_mm, and 2 more selected inputs?",
          "readingCue": "Confirm that X contains these selected inputs and that the reference target stays out of the grouping decision."
        },
        {
          "id": "explore",
          "title": "Explore the data",
          "question": "What ranges and overlaps among bill_length_mm, bill_depth_mm, and 2 more selected inputs might shape groups in Palmer Penguins · All features continuous?",
          "readingCue": "Use the input ranges and overlap to form a first description of possible groups before fitting K-Means."
        },
        {
          "id": "prepare",
          "title": "Prepare the data",
          "question": "How should bill_length_mm, bill_depth_mm, and 2 more selected inputs be scaled so distance comparisons treat Palmer Penguins · All features continuous rows fairly?",
          "readingCue": "Check that the named scaling recipe puts the selected inputs on comparable scales before distances are calculated."
        },
        {
          "id": "compare",
          "title": "Compare possible group counts",
          "question": "Which candidate group count gives a useful balance for Palmer Penguins · All features continuous?",
          "readingCue": "Use inertia, silhouette, cluster sizes, and original-unit profiles together; choose a useful explanation rather than the smallest score alone."
        },
        {
          "id": "fit",
          "title": "Fit K-means",
          "question": "What does the selected starting group count produce for Palmer Penguins · All features continuous?",
          "readingCue": "Compare the starting group count with the candidate evidence, then edit selected_k if another grouping answers this route's question better."
        },
        {
          "id": "diagnose",
          "title": "Check the clusters",
          "question": "Are the discovered Palmer Penguins · All features continuous groups separated enough and large enough to describe?",
          "readingCue": "Read the global silhouette beside the full group counts, then decide whether a small group is meaningful, rare, or fragmented."
        },
        {
          "id": "profile",
          "title": "Explain the clusters",
          "question": "What do these groups look like in the original units of bill_length_mm, bill_depth_mm, and 2 more selected inputs for Palmer Penguins · All features continuous?",
          "readingCue": "Use the original-unit means to describe at least two groups; cluster IDs are labels and do not supply the meaning."
        },
        {
          "id": "visualise",
          "title": "Map the clusters",
          "question": "Describe two groups in the original units of bill_length_mm, bill_depth_mm, and 2 more selected inputs for Palmer Penguins · All features continuous; why is this grouping useful for the selected scenario?",
          "readingCue": "Use the profile and map together, state two concrete group descriptions, and name what the two-dimensional view leaves out."
        }
      ]
    },
    "hierarchical": {
      "dataset": "breast",
      "scenario": "continuous5",
      "steps": [
        {
          "id": "frame",
          "title": "Choose what to discover",
          "question": "How do Breast Cancer Wisconsin (Diagnostic) · All features continuous rows join into a hierarchy when compared on radius_mean, texture_mean, and 3 more selected inputs?",
          "readingCue": "Confirm that X contains these selected inputs and that the reference target stays out of the hierarchy."
        },
        {
          "id": "explore",
          "title": "Explore the data",
          "question": "What ranges and overlaps among radius_mean, texture_mean, and 3 more selected inputs might shape the Breast Cancer Wisconsin (Diagnostic) · All features continuous hierarchy?",
          "readingCue": "Use the input ranges and overlap to describe possible similarity before building the hierarchy."
        },
        {
          "id": "prepare",
          "title": "Prepare the data",
          "question": "How should radius_mean, texture_mean, and 3 more selected inputs be scaled, and why does Breast Cancer Wisconsin (Diagnostic) · All features continuous use a reproducible sample?",
          "readingCue": "Check the comparable scales, the named X_sample frame, and the displayed sample size before pairwise linkage is built."
        },
        {
          "id": "dendrogram",
          "title": "Build the dendrogram",
          "question": "Where do Breast Cancer Wisconsin (Diagnostic) · All features continuous rows join at noticeably different heights before a cut is chosen?",
          "readingCue": "Read leaves as sampled rows or small groups, joins as merges, and large height gaps as candidate cut evidence."
        },
        {
          "id": "compare",
          "title": "Compare possible cuts",
          "question": "Which dendrogram cut gives a useful description of Breast Cancer Wisconsin (Diagnostic) · All features continuous?",
          "readingCue": "Use merge heights, silhouette, sampled group sizes, and original-unit profiles together; silhouette supports the cut but does not decide it."
        },
        {
          "id": "fit",
          "title": "Fit the hierarchy",
          "question": "What does the selected starting cut produce for Breast Cancer Wisconsin (Diagnostic) · All features continuous?",
          "readingCue": "Compare the starting cut with the dendrogram and candidate evidence, then edit selected_k if another cut answers this route's question better."
        },
        {
          "id": "profile",
          "title": "Explain the groups",
          "question": "What do the sampled groups look like in the original units of radius_mean, texture_mean, and 3 more selected inputs for Breast Cancer Wisconsin (Diagnostic) · All features continuous?",
          "readingCue": "Use the sampled original-unit means to describe at least two groups and keep the sample scope in your interpretation."
        },
        {
          "id": "visualise",
          "title": "Map the hierarchy",
          "question": "Describe two sampled groups in the original units of radius_mean, texture_mean, and 3 more selected inputs for Breast Cancer Wisconsin (Diagnostic) · All features continuous; why is this hierarchy useful for the selected scenario?",
          "readingCue": "Use the profile and map together, state two concrete group descriptions, and remember that the hierarchy represents the reproducible sample."
        }
      ]
    },
    "pca": {
      "dataset": "breast",
      "scenario": "continuous30",
      "steps": [
        {
          "id": "frame",
          "title": "Choose what to discover",
          "question": "How can I summarise variation in Breast Cancer Wisconsin (Diagnostic) · All features continuous rows with fewer coordinates from radius_mean, texture_mean, and 28 more selected inputs?",
          "readingCue": "Confirm that the selected inputs define X and that no reference target is used to learn the PCA axes."
        },
        {
          "id": "explore",
          "title": "Explore redundancy",
          "question": "Which selected inputs may overlap in Breast Cancer Wisconsin (Diagnostic) · All features continuous before they are replaced by principal-component coordinates?",
          "readingCue": "Use the named pair to describe possible redundancy before PCA searches for directions of high variance."
        },
        {
          "id": "prepare",
          "title": "Scale the inputs",
          "question": "Why should radius_mean, texture_mean, and 28 more selected inputs share a common scale before PCA compares their variation?",
          "readingCue": "Check that the named scaling recipe prepares each selected input before PCA learns the axes."
        },
        {
          "id": "variance",
          "title": "Fit PCA and inspect explained variance",
          "question": "How much variation does each component represent for Breast Cancer Wisconsin (Diagnostic) · All features continuous, and where does adding another component become less useful?",
          "readingCue": "Read the per-component and cumulative variance for this selected scenario; the next step makes the retention trade-off explicit."
        },
        {
          "id": "select",
          "title": "Select components",
          "question": "How many coordinates should represent Breast Cancer Wisconsin (Diagnostic) · All features continuous under the chosen variance-retention rule?",
          "readingCue": "Check the first component count reaching the active target and record both the count and the retained variation."
        },
        {
          "id": "loadings",
          "title": "Understand the component loadings",
          "question": "Which original inputs shape the first axes for Breast Cancer Wisconsin (Diagnostic) · All features continuous, and how should their signs be read?",
          "readingCue": "Compare absolute loading sizes for contribution and signs for direction; the full table keeps every selected input available for interpretation."
        },
        {
          "id": "project",
          "title": "Project the rows",
          "question": "How many coordinates did you keep for Breast Cancer Wisconsin (Diagnostic) · All features continuous, how much variation do they preserve, and what do the first two axes describe?",
          "readingCue": "Use components_for_target and variance_retained, connect the largest absolute loadings to PC1 and PC2, and name what later components may hide."
        }
      ]
    }
  }
}
